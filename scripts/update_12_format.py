# -*- coding: utf-8 -*-
with open('/Users/ysrtc/Desktop/myNews/task/morning_news/12_GitHub实用Skills.md') as f:
    c = f.read()

start = '## 📝 输出格式（学习文档版）'
end = '## 🚀 执行步骤（自动化系统调用指南）'

si = c.find(start)
ei = c.find(end)

old_section = c[si:ei]

new_section = '''## 📝 输出格式

> ⚠️ **核心要求**：每条内容要让读者看完后真正**理解**这个项目，而不是只知道名字。每个段落必须有具体的例子、数据、对比或原理说明，禁止写「提升了效率」「值得关注」这种空洞套话。宁可只写 8 条有深度的，也不要写 20 条空洞的。

```markdown
### 1. 【标题】（20字以内）

> 📍 **导语**：用 2-3 句话说清楚这个项目是做什么的、之前大家用什么方案有什么痛点、它比旧方案好在哪里。

---

**① 它解决了什么问题？（Before → After）**

先描述这个项目出现之前，开发者做同样的事要怎么做、有什么痛苦。然后说这个项目改变了什么。

必须举具体例子。比如：
- **Before**：以前你要做 X 事，需要手动 A→B→C 三步，耗时 30 分钟，还容易出错
- **After**：现在用这个工具，一条命令搞定，耗时 10 秒，准确率 99%

**② 它的核心原理是什么？（用大白话讲清楚）**

不要空洞地说「基于先进的 AI 技术」。要说清楚：
- 它用了什么模型/算法？（给出名字和出处）
- 输入 → 处理 → 输出，每一步具体发生了什么？
- 架构上分几个模块？各模块负责什么？
- 如果涉及流程，用文字描述一个「从输入到输出」的完整链条

**③ 具体怎么用？（安装 + 核心代码）**

给出完整安装命令和一段可运行的示例代码。代码必须有真实的 API 调用逻辑，每段代码旁边用中文注释说明在做什么。

```python
# 第 1 步：初始化（说明每个参数的作用）
client = SomeClient(api_key="xxx", model="gpt-4")

# 第 2 步：调用核心功能（说明输入是什么、预期输出是什么）
result = client.process(input_data="你好世界")
print(result)  # 输出: 处理后的结果
```

**④ 哪些场景用得上？（至少 2 个具体场景）**

每个场景按这个格式：
- **场景**：比如「用 LangChain 做智能客服」
- **以前怎么做**：写正则匹配规则、维护 FAQ 列表，耗时 1 周，覆盖不到 50% 的问题
- **现在怎么做**：用这个工具，3 步搭建 RAG 流程，半天上线
- **实际效果**：给出量化数据，如「问题解决率从 45% 提升到 85%，响应时间从 5 分钟降到 10 秒」

**⑤ 它比同类项目强在哪？（对比 1-2 个竞品）**

从以下几个维度对比，每个维度用一句话加数据说明：

- **安装/上手难度**：比如 vs 竞品 A，依赖少多少、有没有 Docker 镜像
- **性能**：速度、内存、准确率等具体指标对比
- **功能**：比竞品多了什么功能、少了什么
- **社区/生态**：Star 数、插件数量、文档质量对比
- **适用场景**：什么场景选它、什么场景选竞品

**⑥ 怎么开始学？（前置基础 + 学习路线）**

- **前置知识**：需要掌握什么？（具体到技术名词）
- **入门**（30 分钟）：看哪篇文档/视频能最快跑通
- **实战**（1-2 天）：做什么小项目能巩固理解
- **进阶**（1-2 周）：深入哪个方向能掌握核心能力

---

🔗 **信息来源：** GitHub Repository / GitHub Trending / Hacker News / Reddit / 媒体报道（注明具体日期）
```'''

c = c[:si] + new_section + c[ei:]

with open('/Users/ysrtc/Desktop/myNews/task/morning_news/12_GitHub实用Skills.md', 'w') as f:
    f.write(c)

print("✅ 12 更新成功")
print(f"替换了 {len(old_section)} 字符 → {len(new_section)} 字符")
