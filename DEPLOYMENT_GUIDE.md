# 🖥️ 早间新闻生成 - Gitee Go 流水线部署指南

> 本指南帮助你将早间新闻生成任务部署到 Gitee Go 流水线，实现完全免费的自动化运行。

---

## 📋 方案概览

```
Gitee Go 流水线（免费云端运行）
    ↓ 每天北京时间 08:00 自动触发
Python 脚本运行
    ↓ 调用搜索 API + AI API
生成 .md 新闻文件
    ↓ 自动 commit & push
Gitee Pages 网站更新（可选）
```

**每月费用**：约 ¥2（仅 AI API 调用费用）

---

## 🚀 快速部署步骤

### 第一步：获取 API Key

#### 1. DeepSeek API Key（推荐，便宜）

1. 访问 [https://platform.deepseek.com/](https://platform.deepseek.com/)
2. 注册账号并登录
3. 进入「API Keys」页面
4. 点击「Create API Key」
5. 复制生成的 Key（格式：`sk-xxxxxxxx`）

**价格**：约 ¥1.1/百万输出 tokens
**估算**：每天生成 17 个板块，约消耗 6-8 万 tokens，每天费用约 ¥0.07，每月 ¥2

#### 2. Tavily 搜索 API Key（免费）

1. 访问 [https://app.tavily.com/](https://app.tavily.com/)
2. 用 Google 账号登录
3. 首页就能看到 API Key（格式：`tvly-xxxxxxxx`）

**免费额度**：1000 次/月（每天生成一次，够用）

---

### 第二步：在 Gitee 配置流水线变量

1. 打开你的 Gitee 仓库：`https://gitee.com/maxinde/myNews`
2. 点击左侧 **流水线** 菜单
3. 点击 **变量配置** 标签
4. 点击 **添加变量**

添加以下变量：

| 变量名称 | 值 | 说明 |
|---------|-----|-----|
| `DEEPSEEK_API_KEY` | sk-c050e535985240939ef812a4149a071b | 你的 DeepSeek API Key |
| `TAVILY_API_KEY` | tvly-你的Key | 你的 Tavily API Key |

> ⚠️ 建议勾选「加密」选项，保护你的 API Key 安全

---

### 第三步：启用 Gitee Go 流水线

1. 在流水线页面，点击 **新建流水线**
2. 选择 **模板**，可以选「空白流水线」
3. 或选择「根据 gitee.yml 自动生成」
4. 配置完成后，点击 **保存并执行**

首次启用可能需要：
- 开通 Gitee Go 服务（个人设置 → 功能设置 → Gitee Go）
- 实名认证（免费）

---

### 第四步：验证部署

1. 进入 **流水线** 页面
2. 点击 **立即执行** 手动触发一次测试
3. 查看运行日志，确认是否成功

---

### 第五步：查看运行日志

1. 点击某次运行的记录
2. 点击具体的 Stage
3. 可以看到每一步的日志输出

如果运行失败，日志会显示具体的错误信息。

---

## ⏰ 定时任务配置

当前配置：**每天 UTC 00:00 = 北京时间 08:00**

修改 `.gitee/workflows/daily-news.yml` 中的 cron 表达式：

```yaml
triggers:
  - cron: '0 0 * * *'  # 每天 UTC 00:00 = 北京时间 08:00
```

### 常用定时设置

| 执行时间（北京时间） | cron 表达式 |
|-------------------|-------------|
| 每天 06:00 | `0 22 * * *` |
| 每天 07:00 | `0 23 * * *` |
| 每天 08:00 | `0 0 * * *` |
| 每天 09:00 | `0 1 * * *` |
| 每周一 08:00 | `0 0 * * 1` |
| 每周末 08:00 | `0 0 * * 6` |

---

## 📁 生成的文件结构

运行后，会在 `news/` 目录下生成按月份组织的新闻文件：

```
news/
├── 202604/
│   ├── 20260401_早间.md
│   ├── 20260402_早间.md
│   └── ...
└── 202605/
    └── ...
```

---

## 🔧 本地测试

如果你想在本地测试脚本：

```bash
# 1. 安装依赖
pip3 install -r requirements.txt

# 2. 复制配置示例
cp .env.example .env

# 3. 编辑 .env，填入你的 API Key
nano .env

# 4. 运行脚本
python3 scripts/generate_news_server.py
```

---

## ❓ 常见问题

### Q1: 运行失败怎么办？

查看流水线日志，常见错误：
- **API Key 无效**：检查流水线变量配置是否正确
- **API 额度用完**：登录对应平台检查用量
- **找不到脚本**：确保 `scripts/generate_news_server.py` 路径正确

### Q2: 如何增加板块或修改关键词？

编辑 `scripts/generate_news_server.py` 中的 `SECTIONS` 列表。

### Q3: 可以生成周报吗？

可以，创建一个 `weekly-news.yml` 工作流，修改 cron 表达式为每周执行即可。

### Q4: Gitee Go 需要付费吗？

- 基础版：每月 1000 分钟免费构建时长
- 个人版：每月 500 分钟免费（实名认证后）
- 对于你的任务（每天约 10-30 分钟），完全够用

### Q5: 可以推送到其他平台吗？

可以，修改 `generate_news_server.py` 中的 `push_to_github()` 函数，添加微信推送、邮件通知等。

---

## 📊 费用估算

| 项目 | 费用 | 说明 |
|------|------|------|
| Gitee Go 流水线 | 免费 | 每月500-1000分钟 |
| Tavily 搜索 | 免费 | 1000次/月 |
| DeepSeek AI | ~¥2/月 | 约 7万 tokens/天 |
| **合计** | **~¥2/月** | |

---

## 🎯 下一步优化

1. **添加微信推送**：接入企业微信/钉钉机器人，每日推送链接
2. **添加邮件通知**：运行完成后发送邮件
3. **生成周报**：`weekly-news.yml` 工作流
4. **优化 AI 提示词**：进一步提升生成质量
5. **添加更多板块**：根据需要扩展

---

## 📞 获取帮助

如果遇到问题：
1. 查看流水线日志中的具体错误信息
2. 检查 API Key 是否正确配置
3. 访问 [Gitee Go 文档](https://gitee.com/help/articles/4290) 查阅帮助

---

> 💡 **提示**：部署完成后，建议先手动运行几次测试，确认一切正常后再依赖定时任务自动运行。
